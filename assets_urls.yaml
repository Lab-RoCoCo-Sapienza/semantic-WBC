# External asset URLs — not stored in git.
# Upload zip via: ./package_assets.sh → attach to GitHub Release

version: "1.5.0"

base_url: "https://github.com/Lab-RoCoCo-Sapienza/semantic-WBC/releases/download/v1.5.0"

bundles:
  full:
    file: robojudo-assets-v1.5.0.zip
    size_mb: 109
    sha256: ""
    description: >
      Models (ONNX, Unitree .pt), MuJoCo G1 meshes, mp3_songs, Shazam indices, demo mashup.
